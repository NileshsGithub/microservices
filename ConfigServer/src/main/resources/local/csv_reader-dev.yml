spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Person
    username: postgres
    password: Nilesh@2002
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true